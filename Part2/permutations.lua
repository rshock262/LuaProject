function swap(char one, char two)
    char temp
    temp = one
    one = two
    two = temp
end

function permute(char uno, int l, int r)
    int i
    if(l == r)
    print(" \n",uno)
    else
--TODO        for(i=l; i <=)